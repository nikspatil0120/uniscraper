export function NotFound() {
  return (
    <span
      style={{
        color: "#4A4958",
        fontStyle: "italic",
        fontFamily: "inherit",
      }}
    >
      — not found
    </span>
  );
}